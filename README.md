# RIS/FIM辅助ISAC与通信论文对比

| 对比项目 | 论文1 | 论文2 | 论文3 | 论文4 | 论文5 | 论文6 |
|---|---|---|---|---|---|---|
| **论文标题** | **RIS-Aided Integrated Sensing and Communication: Joint Beamforming and Reflection Design** | **Joint Transceiver Beamforming and Reflecting Design for Active RIS-Aided ISAC Systems** | **Cramér–Rao Bound Optimization for Active RIS-Empowered ISAC Systems** | **Flexible Intelligent Metasurfaces for Downlink Multiuser MISO Communications** | **Active Sensing for Multiuser Beam Tracking With Reconfigurable Intelligent Surface** | **Physics-Informed Implicit Neural Representation for Wireless Imaging in RIS-Aided ISAC System** |
| **IEEE链接** | [IEEE入口](https://ieeexplore.ieee.org/document/10052711/) | [IEEE入口](https://ieeexplore.ieee.org/document/10054402/) | [IEEE入口](https://ieeexplore.ieee.org/document/10496515/) | [IEEE入口](https://ieeexplore.ieee.org/document/10850658/) | [IEEE入口](https://ieeexplore.ieee.org/document/10758383/) | [IEEE入口](https://ieeexplore.ieee.org/document/11400690/) |
| **研究背景** | 遮挡和信道衰落会同时削弱通信信号与目标回波。被动RIS能够产生额外的可控传播路径，提高通信和感知性能。 | 被动RIS存在严重的乘性衰落。当BS与目标之间的直达链路被阻挡时，目标回波可能非常弱，因此引入可以放大信号的Active RIS。 | 雷达SNR主要描述目标检测能力，不能直接表示目标参数估计精度，因此需要使用CRB评价目标DoA估计性能。 | 固定平面阵列无法根据传播环境改变天线位置。在毫米波环境中，小幅位置变化也可能显著改变信道相位和衰落。 | 完整估计RIS级联信道需要大量导频。固定或随机RIS感知模式无法利用历史信道信息，也不能主动关注用户可能出现的区域。 | FT和CS成像依赖准确的传播模型和多径分离；普通数据驱动深度学习又需要大量CSI—真实图像训练样本。 |
| **论文目的** | 联合优化BS发射波束和RIS反射系数，在满足通信QoS及功率限制的同时，最大化多个目标检测SNR的加权和。 | 联合优化BS发射波束、雷达接收滤波器和Active RIS反射系数，最大化雷达输出SNR。 | 联合优化BS发射预编码和Active RIS反射系数，使目标DoA估计的CRB最小。 | 联合优化BS发射波束和FIM表面形状，在满足用户SINR要求的情况下最小化BS总发射功率。 | 根据历史导频自适应设计RIS感知系数，并联合设计下行RIS系数、AP波束和功率分配，最大化多用户最低速率。 | 使用INR表示连续散射图像，通过无线传播物理模型和实测CSI训练网络，实现无图像标签的二维或三维无线成像。 |
| **系统模型** | 多天线双功能BS在被动RIS辅助下，同时服务多个单天线用户并检测多个点目标。 | 多天线BS通过Active RIS服务多个通信用户，并检测被障碍物遮挡的点目标。 | 多天线BS通过Active RIS服务多个通信用户，同时估计一个被遮挡点目标的DoA。 | BS部署由多个可移动辐射单元组成的FIM，并向多个单天线用户进行下行传输。 | 多天线AP通过被动RIS服务多个移动用户。每帧由上行导频阶段和下行数据传输阶段组成。 | 全双工BS使用独立TX和RX阵列、被动RIS及OFDM信号，对ROI中的扩展目标进行成像。 |
| **系统包含什么** | M天线BS<br><br>N单元被动RIS<br><br>K个单天线用户<br><br>T个点目标 | 多天线BS<br><br>Active RIS<br><br>K个通信用户<br><br>一个点目标<br><br>雷达接收滤波器 | 多天线BS<br><br>Active RIS<br><br>K个通信用户<br><br>一个被遮挡点目标 | N单元FIM<br><br>多天线BS<br><br>K个单天线用户<br><br>多条传播路径和散射体 | M天线AP<br><br>N<sub>r</sub>单元RIS<br><br>K个移动用户<br><br>导频阶段和数据阶段 | N<sub>t</sub>根TX天线<br><br>N<sub>r</sub>根RX天线<br><br>N<sub>s</sub>单元RIS<br><br>通信用户<br><br>ROI及扩展目标 |
| **感知场景** | 多点目标检测 | 受阻挡点目标检测 | 点目标DoA参数估计 | 无雷达感知场景；该论文研究纯下行通信 | 时变通信信道主动感知与多用户波束跟踪 | 扩展目标二维或三维无线成像 |
| **感知输出** | 每个目标的检测SNR | 雷达输出SNR | 目标DoA θ及其CRB | 不适用 | 自适应RIS感知模式、下行RIS模式和用户波束 | 连续空间散射函数 σ(x,y,z)及二维/三维散射图像 |
| **目标模型** | T个点目标 | 一个点目标 | 一个点目标，未知参数为<br><br><code>ξ = [θ, Re{α}, Im{α}]ᵀ</code> | 无感知目标 | K个移动通信用户 | 扩展目标被划分为N<sub>v</sub>个体素：<br><br><code>σ = [σ₁, σ₂, …, σNv]ᵀ</code> |
| **RIS/FIM类型** | 被动RIS | Active RIS | Active RIS | 部署在BS上的Active FIM | 被动RIS | 被动RIS |
| **RIS/FIM系数** | <code>Φ = diag(φ)</code><br><br><code>φₙ = exp(jθₙ)</code> | <code>Φ = diag(φ)</code><br><br><code>φₘ = aₘexp(jθₘ)</code> | <code>Φ = diag(φ)</code><br><br><code>φₘ = aₘexp(jθₘ)</code> | 没有传统RIS反射系数。可控变量为FIM形状：<br><br><code>y = [y₁, y₂, …, yN]ᵀ</code> | 感知系数：<code>vₗ⁽ᵗ⁾</code><br><br>下行传输系数：<code>w⁽ᵗ⁾</code> | K组RIS配置：<br><br><code>Ω = [ω₁, ω₂, …, ωK]ᵀ</code> |
| **RIS/FIM物理限制** | <code>abs(φₙ) = 1</code> | <code>aₘ ≤ amax</code><br><br>同时满足Active RIS总功率限制 | <code>aₘ ≤ amax</code><br><br>同时满足Active RIS总功率限制 | <code>0 ≤ yₙ ≤ ymax</code> | <code>abs([vₗ⁽ᵗ⁾]ᵢ) = 1</code><br><br><code>abs([w⁽ᵗ⁾]ᵢ) = 1</code> | <code>abs(ωk,n) = 1</code> |
| **代表性接收信号/测量模型** | <code>yₖ = hₖᴴ(φ)Ws + nₖ</code> | <code>yₖ = hₖᵀ(φ)Ws + hᵣ,ₖᵀΦz + nₖ</code> | <code>yₖ = hₖᵀ(φ)Ws + hᵣ,ₖᵀΦz + nₖ</code> | <code>rₖ = hₖᴴ(y) Σk' wₖ'sₖ' + nₖ</code> | <code>Ȳₖ⁽ᵗ⁾ = Aₖ⁽ᵗ,⁰⁾V⁽ᵗ⁾ + Z̄ₖ⁽ᵗ⁾</code> | <code>y = f(σ, Ω) + n</code> |
| **主要优化变量** | BS发射波束矩阵W<br><br>RIS反射向量φ | BS发射波束矩阵W<br><br>Active RIS系数φ<br><br>雷达接收滤波器u | BS发射预编码矩阵W<br><br>Active RIS系数φ | 用户波束向量{wₖ}<br><br>FIM表面形状y | 上行主动感知策略F⁽ᵗ⁾<br><br>下行RIS策略G⁽ᵗ⁾<br><br>功率分配策略H⁽ᵗ⁾ | INR神经网络参数θ<br><br>多径扩展中还包括未知多径矩阵A |
| **优化目标** | 最大化多个目标检测SNR的加权和 | 最大化雷达输出SNR | 最小化目标DoA估计CRB | 最小化BS总发射功率 | 最大化期望平均最低用户速率 | 最小化预测CSI与实测CSI的误差，并对图像进行稀疏正则化 |
| **完整主优化问题** | <img width="224" height="141" alt="image" src="https://github.com/user-attachments/assets/ceee2be2-ecb8-41e8-b93e-1aaddfdf71c7" /> | <img width="230" height="155" alt="image" src="https://github.com/user-attachments/assets/5173c976-107f-4683-965c-3894c82b37dc" />| <img width="248" height="155" alt="image" src="https://github.com/user-attachments/assets/3fe6065d-e227-4834-9168-81187437aca5" /> |<img width="347" height="184" alt="image" src="https://github.com/user-attachments/assets/dd0a5df6-8ae4-43fb-af32-e0065f10e6a3" /> | <img width="400" height="290" alt="image" src="https://github.com/user-attachments/assets/98042b89-29e6-4668-8b7c-32c146c783dd" /> |<img width="470" height="299" alt="image" src="https://github.com/user-attachments/assets/d4097657-d207-45b3-8d6d-7e41a1979ced" />|
| **CRB/损失的具体表达** | 优化多个目标的加权检测SNR：<br><br><code>Σₜ ωₜγₜ</code> | 雷达SNR由目标回波功率与干扰、RIS噪声及接收噪声之比构成 | <code>CRBθ = [Mθθ - Mθα Mαα⁻¹ Mθαᵀ]⁻¹</code> | <code>Pt = Σₖ norm₂(wₖ)²</code> | 训练损失为目标函数的负值：<br><br><code>Loss = -(1/U) Σₜ E[(1/N) Σₙ minₖ Rₖ⁽ᵗ,ⁿ⁾]</code> | <code>Lθ = norm₂(ŷθ-y) + α norm₁(σ̂θ)</code> |
| **通信约束** | <code>γₖᶜᵒᵐ ≥ Γₖ</code> | <code>γₖ ≥ Γₖ</code> | <code>SINRₖ ≥ γₖ</code> | <code>SINRₖ ≥ γₖ</code> | 通过目标中的<code>minₖ Rₖ</code>保证用户公平性 | 核心成像问题没有通信QoS约束 |
| **通信约束代表什么** | 每个通信用户都必须达到最低SINR要求 | 不能为了提高雷达SNR而牺牲用户通信质量 | 在最小化CRB的同时保证所有用户QoS | 在降低发射功率时仍需满足每个用户的数据速率要求 | 防止算法只提高信道较好用户的速率而忽略弱用户 | P2主要研究图像重建；通信增强在论文后续部分单独处理 |
| **BS功率约束** | <code>‖W‖²<sub>F</sub> ≤ P<sub>max</sub></code> | <code>‖W‖²<sub>F</sub> ≤ P<sub>BS</sub></code> | <code>‖W‖²<sub>F</sub> ≤ P<sub>BS</sub></code> | BS总发射功率是目标函数，而不是单独的上限约束 | <code>Σ<sub>k=1</sub><sup>K</sup> p<sub>k</sub><sup>(t)</sup> = P<sub>d</sub></code> | BS发射功率在成像问题中作为固定系统参数 |
| **BS功率约束代表什么** | BS总发射功率不能超过预算 | 限制通信和雷达共用波形的总发射功率 | 限制通信波束与雷达波形的总发射功率 | 寻找满足全部用户要求所需的最小功率 | AP分配给所有用户的总功率等于可用下行功率 | 论文核心优化的是图像网络，而不是BS波束和发射功率 |
| **RIS功率约束** | 无；被动RIS不主动放大信号 | <code>P(W,φ) ≤ PRIS</code> | <code>P(W,φ) ≤ PRIS</code> | 不适用 | 无；使用被动RIS | 无；使用被动RIS |
| **RIS功率约束代表什么** | 不适用 | 限制Active RIS放大入射信号和RIS噪声所消耗的总功率 | 限制Active RIS对发射信号、目标回波和热噪声的放大功率 | 不适用 | 不适用 | 不适用 |
| **RIS/FIM硬件约束** | <code>abs(φₙ) = 1</code> | <code>aₘ ≤ amax</code> | <code>aₘ ≤ amax</code> | <code>0 ≤ yₙ ≤ ymax</code> | <code>abs([vₗ⁽ᵗ⁾]ᵢ) = 1</code><br><br><code>abs([w⁽ᵗ⁾]ᵢ) = 1</code> | <code>abs(ωk,n) = 1</code> |
| **硬件约束代表什么** | 被动RIS只能改变相位，不能放大信号 | 每个Active RIS单元的放大倍数受硬件能力限制 | 防止Active RIS单元产生不现实的无限放大增益 | 每个FIM辐射单元只能在机械装置允许的范围内移动 | 被动RIS的感知模式和传输模式都只能改变相位 | 被动RIS只能改变入射信号的相位 |
| **其他条件或先验** | 不同目标通过权重ωₜ表示不同重要程度 | 需要考虑Active RIS引入并放大的热噪声 | FIM和CRB都取决于目标角度、复反射系数以及Active RIS噪声 | 假设BS和用户具有完美CSI | 当前策略只能使用当前及历史导频，不能使用未来真实CSI | 正向模型必须可微；网络输出非负；图像使用稀疏先验 |
| **问题特点** | 发射波束与RIS系数高度耦合，并包含单位模约束，因此属于非凸问题 | W、φ和u高度耦合；雷达SNR为分式高阶非凸函数 | CRB包含矩阵求逆、分式项和耦合变量，属于非凸优化 | SINR约束非凸，且波束与FIM形状高度耦合 | 需要优化三个高维时序策略函数，无法直接获得解析解 | 无真实图像标签的非凸神经逆问题，需要针对每个场景在线训练 |
| **优化方法** | 交替优化AO（联合协调更新W 和 $\boldsymbol\phi$）<br><br>SCA/MM（优化BS发射波束矩阵W）<br><br>罚函数与辅助变量法（解耦RIS反射系数 $\boldsymbol\phi$）<br><br>黎曼共轭梯度RCG（优化单位模RIS反射系数 $\boldsymbol\phi$） | 块坐标下降BCD（交替更新 u、W 和 $\boldsymbol\phi$）<br><br>广义Rayleigh商/闭式解（优化雷达接收滤波器u）<br><br>Dinkelbach变换与MM（优化BS发射波束矩阵W）<br><br>Dinkelbach变换、Kronecker积与MM（优化Active RIS反射系数 $\boldsymbol\phi$）<br><br>CVX（求解W 和 $\boldsymbol\phi$ 对应的凸近似子问题） | 交替优化AO（交替更新BS预编码矩阵 $\mathbf W$ 和Active RIS系数 $\boldsymbol\phi$）<br><br>Schur补与SDR/SDP（优化BS发射预编码矩阵 $\mathbf W$）<br><br>MM/SCA（优化Active RIS反射系数 $\boldsymbol\phi$）<br><br>RCG黎曼共轭梯度（初始化Active RIS反射系数 $\boldsymbol\phi^{(0)}$）<br><br>CVX（求解 $\mathbf W$ 和 $\boldsymbol\phi$ 的凸化子问题） | 交替优化AO（交替更新用户波束 $\{\mathbf w_k\}$ 和FIM形状 $\mathbf y$）<br><br>上下行对偶与固定点迭代（优化MMSE波束 $\{\mathbf w_k\}$）<br><br>ZF方法（获得低复杂度波束 $\{\mathbf w_k\}$）<br><br>投影梯度法（优化FIM表面形状 $\mathbf y$） | LSTM（更新并表示各用户的时序信道状态 $\{\mathbf c_k^{(t)},\mathbf h_k^{(t)}\}$）<br><br>GNN感知节点（生成RIS感知向量 $\{\mathbf v_\ell^{(t+1)}\}$）<br><br>GNN传输节点（生成下行RIS系数 $\mathbf w^{(t)}$）<br><br>GNN用户节点（优化功率分配 $\{\mathbf p^{(t)},\boldsymbol\lambda^{(t)}\}$）<br><br>解析波束结构（计算AP波束 $\{\mathbf b_k^{(t)}\}$）<br><br>Adam与反向传播（联合优化LSTM和GNN的全部网络参数） | Fourier位置编码（处理INR输入坐标 $\mathbf p$，本身不是独立优化变量）<br><br>正弦激活MLP（参数化连续散射函数 $\mathcal M_{\boldsymbol\theta}$）<br><br>反向传播与梯度优化（优化INR参数 $\boldsymbol\theta$）<br><br>联合梯度优化（在多径扩展P3中优化 $\boldsymbol\theta$ 和未知多径矩阵 $\mathbf A$）<br><br>上一帧参数初始化（初始化动态成像参数 $\boldsymbol\theta_t^{(0)}=\boldsymbol\theta_{t-1}^{\star}$） |
| **多径处理方式** | 利用RIS产生额外可控传播路径，但没有专门学习未知多径 | 利用Active RIS增强虚拟BS—RIS—目标传播路径 | 利用Active RIS增强被遮挡目标的双程回波路径 | 通过改变阵列物理形状，使不同多径形成更有利的叠加 | 从历史导频中学习时变信道，不需要显式估计完整级联信道 | 背景校准去除与ROI无关的多径；使用未知矩阵A联合学习目标相关多径 |
